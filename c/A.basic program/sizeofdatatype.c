#include <stdio.h>
int main()
{
    int i;
    float f;
    char c;
    printf(" size of int = %d bytes\n", sizeof(i));
    printf("size of float = %d bytes\n", sizeof(f));
    printf("size of char = %d bytes\n", sizeof(c));
    return 0;
}
